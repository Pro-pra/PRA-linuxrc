#!/bin/sh

echo && echo
echo "[1;36m""Модули дистрибутива не найдены. Скорее всего Вы используете не поддерживаемое загрузочное устройство (SCSI или старый PCMCIA).
Решение: Скопируйте папку /$FOLDER с вашего загрузочного устройства на IDE/SATA диск, например, в /mnt/sda1/$FOLDER или C:\\$FOLDER
И попробуйте загрузить снова.
Удостоверьтесь что Ваши параметры загрузки (cheatcodes) верны.
При сетевой загрузке - проверьте включен ли драйвер вашей сетевой карты в образ initrd.

Нажмите пробел/enter для размонтирования всех устройств и перезагрузки или любую клавишу для выхода в отладочную консоль.""[0m"
read -s -n1 ans
if [ "$ans" = "" ]; then
    umount /mnt/* 2>/dev/null
    umount -a 2>/dev/null
    reboot -f
else
    echo -en "[1;36m""Вот ваша отладочная консоль. Размонтируйте все разделы и нажмите Ctrl+Alt+Del\nкогда закончите.\n\nУдачи!\n""[0m"
    sh
fi